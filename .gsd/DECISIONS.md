# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | architecture | MovementValidator shape | Single MovementValidator class with broken-out private methods and an internal PlottingState snapshot. | All rules consult JSON tables; GDScript favors clear procedural code over plugin-style rule engines; debugging interlocking rule failures is easier in one class with explicit method names. Rule-engine of composable rule classes considered and rejected as premature abstraction. | Yes | collaborative |
| D002 | M001 | architecture | Movement resolver placement | New MovementResolver class under scripts/server/, symmetric with MovementValidator. | Clean separation between plotting (legality) and resolution (execution under simultaneity rules). ShipStateController and MovementPlottingController are the wrong concerns. Rejected: extending ShipStateController (bloats one class); method on plotting controller (session manager, not simulator). | Yes | collaborative |
| D003 | M001 | pattern | Mid-resolution user input pattern | Async/await on Godot signals for contested-hex surrender and bear-off prompts during resolution. | Godot-idiomatic, single execution context, no new mini-protocol. Stub-AI prompt answers injected programmatically so deadlock is impossible in single-machine M001. Rejected: mini-session protocol (too heavy for yes/no); explicit state machine (too verbose). | Yes | collaborative |
| D004 | M001 | architecture | Resolver granularity | Impulse-by-impulse simulation: 1 MP per ship per impulse, contests checked per impulse. | Only granularity that makes contested-hex rules behave correctly in fleet engagements. Full-plot-then-contest breaks pass-through semantics between adjacent ships. Matches Close Action mechanics referenced in docs/. | Yes | collaborative |
| D005 | M001 | architecture | Stub-AI plotting via real protocol | Stub-AI drives MovementPlottingController (handle_start_plotting, handle_select_hex, handle_submit_movement) for non-player ships, same as a remote client would. | AI plots are rule-validated; exercises the full plotting stack on AI ships; player and AI travel the same code path. Direct writes to ShipState.plotted_actions rejected because they bypass validation and would silently accept illegal AI plots. | Yes | collaborative |
| D006 | M001 | scenario-schema | RNG determinism for scenarios | Single seeded RandomNumberGenerator on GameState; scenario JSON requires integer `seed` field; `-1` means generate a fresh seed at game start (trace-logged). EnvironmentController migrates to consume GameState.rng. | Deterministic replay for tests/debugging. Required seed makes scenarios reproducible; -1 sentinel preserves play-variability when authors want it. Single source converges existing RNG fragmentation. | Yes | collaborative |
| D007 | M001 | architecture | Resolution playback architecture | Resolver emits a ResolutionLog (per-impulse events + pause-points for prompts); view-side MovementResolutionPlaybackController consumes the log, animates ships hex-by-hex, surfaces prompts via signals. | Resolver stays pure-logic and fully testable. Player still gets the simultaneous-resolution drama. Decouples rule logic from animation timing. Rejected: live-animate as resolver steps (entangles concerns); instant resolution (kills drama). | Yes | collaborative |
| D008 | M001 | convention | DataManager rule-lookup boundary | All JSON rule-table lookups go through typed DataManager.get_* functions with assert() parameter validation, modeled on load_movement_allowance_table() / get_movement_allowance(). Pattern: load_<table>_table() + get_<table>(typed params) + test/unit/test_data_manager_<table>.gd. Any missing wrapper is added before its consumer is written. | Single source of truth for rule lookups; consistent style; catches bad parameters fast; tables are independently testable. This is the established codebase convention; M001 makes it explicit and project-wide. | Yes | human |
| D009 | M001 | testing | Test strategy for M001 | Unit tests for rule math + table lookups (existing test_data_manager_* style) plus scenario-driven integration tests under test/fixtures/scenarios/ driving the full validator → resolver → state-update path. Determinism test runs the same fixture twice with the same seed and diffs the ResolutionLog. All run via `make test`. | Catches math and wiring bugs. Fixtures double as regression coverage for subsequent milestones. CLI test harness considered and rejected as premature scaffolding. | Yes | collaborative |
| D010 | M001/S01 | testing | Treatment of preexisting test failures during M001/S01 | All preexisting failing tests in `make test` (currently 45 failures across test_data_manager_tacking, test_data_manager_speed_change, test_data_manager_ships, test_data_manager_turning) must be fixed before S01 can complete. The S01 verification gate (T03) is no longer "no new regressions"; it is "make test is fully green". | User override (`.gsd/OVERRIDES.md`, 2026-05-11T22:22:15Z, applied at M001/S01/T02) changed the slice exit bar from "no regressions vs current baseline" to "all tests pass, even preexisting failures". Carrying preexisting failures forward would let the verification gate succeed on a red bar and silently accept rule-table bugs that S03+ depends on. Rejected alternatives: (a) defer preexisting failures to a later slice — defeats the override; (b) suppress/skip the failing tests — masks rule-table bugs the validator/resolver will hit. | Yes | human |
| D011 |  | workflow | Whether to skip S01 and advance to S02 | Skip S01 per user override. S01's deliverables (bearing_off_table DataManager wrapper, scenario seed validation, crew_quality normalization) will be folded into S02 as prerequisite work since S02 depends on S01. | User override "continue with the next slice" registered at 2026-05-12T14:37:16Z. S01 had 0/4 tasks completed and no slice directory on disk — no work to preserve. S02's boundary map consumes S01 outputs (validated scenario dict with seed), so S02 planning must absorb those deliverables. | No — S01 is skipped and its deliverables are absorbed into S02. | human |
